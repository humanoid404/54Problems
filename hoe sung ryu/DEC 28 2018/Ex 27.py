
#ex 27 fail .. 

def first_name(f):
    while True:
        f = input("What is your first name?")
        if len(f) < 2:
            continue
        else:
            first = list(f)
            break


def last_name(l):
    while True:
        if len(l) < 2:
            l = input("What is your last name?")
        continue
        else:
        last = list(l)
        break


def Zip_code(z)
    while True:
        try:
            trype(z) == int
            last = list(z)
            break

            else:
            z = input("")

    except:
    print("ZIP code must be numeric")


def create_ID(x, y, z):


